{ default, channels }:
with default; with channels;[
  bashburn                     # Collection of shell scripts for Linux/*BSD for burning discs from the terminal
  bc                           # Arbitrary precision calculator language
  cdrkit                       # Collection of computer programs for CD and DVD authoring
  cifs_utils                   # Utilities for managing mounts of CIFS network file systems
  nixpkgs-master.clac          # Command line, stack-based calculator with postfix notation
  curl                         # Command line tool and library for transferring data with URLs
  dmidecode                    # Tool for dumping a computer's DMI table contents in a human-readable format
  elinks                       # Text mode WWW browser
  file                         # Determine file type
  glxinfo                      # Display info about a GLX extension and OpenGL renderer
  gnupg                        # Implementation of the OpenPGP standard
  hddtemp                      # Utility that gives the hard-drive temperature via S.M.A.R.T.
  htop                         # Interactive process viewer for Unix systems
  iptables                     # Administration tools for IPv4 and IPv6 packet filtering and NAT
  lshw                         # Tool to extract detailed information on the hardware configuration of the machine
  lsof                         # List open files
  manpages posix_man_pages     # Documentation about the Linux kernel and C library interfaces that are employed by user-space programs
  mono                         # Implementation of Microsoft's .NET Framework
  nix-prefetch-scripts         # Scripts used to obtain source hashes for nix-prefetch-*
  nmap                         # Utility for network discovery and security auditing
  nox                          # Tools to make nix nicer to use
  parted                       # Program for creating and manipulating partition tables
  ntfs3g                       # Read-write NTFS driver
  pciutils                     # Programs for listing PCI devices, inspecting their status and setting their configuration registers
  psmisc                       # Miscellaneous utilities that use the proc file-system
  lm_sensors                   # Tools and drivers for monitoring temperatures, voltage, and fans
  rsync                        # Fast, versatile, remote (and local) file-copying tool
  screen                       # Full-screen window manager that multiplexes a physical terminal between several processes
  smartmontools                # S.M.A.R.T. Monitoring Tools
  strace                       # Diagnostic, debugging and instructional userspace utility for Linux
  telnet                       # User interface to the TELNET protocol
  tree                         # List contents of directories in a tree-like format
  unar                         # Archive unpacker program
  unshield                     # Tool and library to extract CAB files from InstallShield installers
  usbutils                     # USB utilities
  vulnix                       # Vulnerability scanner for nix
  wget                         # Utility for non-interactive download of files from the Web
  p7zip                        # Command line port of 7-Zip for POSIX systems
]